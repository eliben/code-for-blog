To run the tool on the sample module:

```
$ go run . -- sample-module
```

Or build the tool first and then invoke the built binary:

```
$ go build -o func-pram-types
./func-param-types sample-module
```
