[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "htmlize"
dependencies = ["pluggy>=0.3,<1.0"]
dynamic = ["version"]

[project.scripts]
htmlize_main = "htmlize.main:main"
