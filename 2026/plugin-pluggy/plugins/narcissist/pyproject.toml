[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "htmlize-narcissist"
dependencies = ["htmlize"]
dynamic = ["version"]

[project.entry-points.htmlize]
narcissist = "narcissist"
